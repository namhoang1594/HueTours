@inherits UmbracoViewPage<SearchingViewModel>
@using Huetours.ViewModels;

@using(Html.BeginUmbracoForm("SubmitForm", "SearchSurface", FormMethod.Post))
            {
                <div class="container">
                    <div class="row">
                        <div class="form-group col-md-6">
                            @Html.LabelFor(m => m.Query)
                            <div class="input-group">
                                @Html.TextBoxFor(m => m.Query, new { @class = "form-control", placeholder = "Search......." })
                            </div>
                        </div>

                        <div class="form-group col-md-6">
                            <label>Search</label>
                            <button id="submit" class="btn btn-success btn-lock">Search</button>
                        </div>
                    </div>

                </div>
            }